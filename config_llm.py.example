"""
Configuración de LLM para generación de observaciones
Copiar este archivo a config_llm.py y configurar con tus credenciales
"""
import os

# Configuración de OpenAI
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "tu-api-key-aqui")
OPENAI_MODEL = "gpt-4o-mini"  # Modelo a usar (gpt-4o-mini es más económico)

# Configuración alternativa (si usas otro proveedor)
# ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY", "")
# GOOGLE_API_KEY = os.getenv("GOOGLE_API_KEY", "")

# Habilitar/deshabilitar generación con LLM
USAR_LLM_OBSERVACIONES = True

# Configuración de rutas de anexos
RUTA_BASE_ANEXOS = "data/anexos"  # Ruta base donde se almacenan los anexos

